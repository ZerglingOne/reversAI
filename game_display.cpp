#include <iostream>
#include "grid.h"

void drawBoard(int states[64])
{
  for(int i = 0; i < 8; i++)//determines rows
    {
      for(int j = 0; j < 5; j++)//the lines of the boxes
      {
        for(int k = 0; k < 8; k++)//determines columns.
      }
    }
}
